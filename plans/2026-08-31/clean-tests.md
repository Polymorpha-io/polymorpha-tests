# Clean Tests — Central Registry Dedup & Flake Fix

## Context & Problem

- What prompted: Audit `2026-08-31` `polymorpha-tests G22` Verification Layer has `fixtures/helpers.ts:114` byte-identical to `suites/polymorpha/tests/mocks/helpers.ts:112` except `dirty_10k`; `fixtures/paths.ts:8` dup `mocks/paths.ts:8` + third copy `g18-concurrency:5 csvPath() resolve(process.cwd,"tests","mocks")`; `fixtures/helpers.ts:25 parseCSV RFC4180 hand-roll +59 detectColumnType numeric>=0.9/date>=0.9 +75 coerceValue` reimplements `papaparse 5.5.4` + `@polymorpha/business-logic typeDetector/IO.Parser` G15/G17; `suites/polymorpha/package.json:14 test:central one-liner C:/Users/shawn/Desktop/...` + `full-pipeline-ml:22 C:/Users/OneDrive/Desktop/df_final_features.csv` + `README.md:9 C:\Users\shawn\Desktop\polymorpha-tests` violates G15 GitHub-only; 3 fixtures `spotify_subset.csv/unicode.csv/single_row.csv` 0 `csvPath`/`fixtures.` hits (`grep csvPath spotify 0`, `helpers.ts` never imports `unicode`); `single_row` never in `helpers.ts` map; `large.csv` on disk not in `fixtures/helpers` map → API `fixtures.large` fails vs `csvPath("large")` filesystem-only inconsistency; `generators/seed.ts:20 hashString(number) FNV 2166136261*16777619` collides with `business-logic hashString Promise<string> hex` + dup `g18-concurrency:26 hashString(number)` third copy; `tests/generators/bl-bridge.ts:28` pass-through `export * from "./dataset"` indirection; `tests/unit/process/transform/*.test.tsx 4 files getMockDataset 12L×4` inline vs `generators/dataset.ts:3` comment says generators replace boilerplate; `e2e/data-modeller-dirty-10k:1354` monolith `A01-K100 15 stories Promise.all 4×` hard to filter; 13× `waitForTimeout 300-15000` (`beginner-rational:56 300 flow 59/325 300/500 stella-knowledge:27 1500 pipeline-smoke:11 3000 data-modeller-dirty:71 300 no-react-error:12 5000 notebook:101 2000 workspace-open:89 15000`) + 10× `.catch(()=>{})` swallow `toHaveCount(0)`; 10× `toHaveScreenshot maxDiffPixels:300` flakes; `page.on("pageerror",console.log)` debug left `pipeline-smoke:8 no-react-error:14`; `scripts/sync.mjs:32 UPSTREAMS 4 entries` (polymorpha twice `dest suites/polymorpha + suites cloud-functions`) but `.sync-sha.json 3 keys` aliased via `if(!shas[repo])`, `console.warn skip missing 161` not G19 throw; `vite.config.ts root` includes `suites/**/*.test.*` but lacks stella alias `@ → suites/polymorpha/src` only vs mirrored `suites/polymorpha/vite.config.ts` drift alias `@mocks`; `ci.yml:58 python pytest suites/business-logic/python only` vs `pyproject.toml:14 testpaths suites/business-logic/python suites/stella/python suites/cloud-functions` stella not CI-covered; `generators` TS `dirty10k` not in py vs py `df_final 30cols×22` not in TS drift; `contract.ts KNOWN_MISSING_BUILDERS 10` churn; `package.json:typescript 5` but no `eslint` scripts `lint eslint .` fails; `exports generators` mis `fixtures/helpers` not exported; `suites/polymorpha/src/` generated ignored via `.gitignore:2` but present on disk bloats grep; `fixtures/dirty_10k.csv 1.2MB` committed vs LFS/generated.
- Current vs desired: Today `vite.config.ts + playwright.config.ts + src/test/setup.ts + src` cycle via `UPSTREAMS raw.githubusercontent` GitHub-only G21 hash truth, but helpers duplication + local fallback + flakes hide contract. Desired: single `fixtures/` source, `papaparse` + business-logic detector reuse, GitHub-only no `C:\`, `waitForResponse` not `waitForTimeout`, split monolith, 4-SHA sync, CI covers all python suites.
- Who / sub-units: All 11 `suites/polymorpha/{unit,api,e2e,mocks,generators}` + `suites/business-logic/python` 347 + `suites/stella/{unit,e2e,python}` + `suites/cloud-functions` + `fixtures/*.csv`.
- Affected Repos: [business-logic ☐ stella ☐ polymorpha ☐ tests ☑] + Reason: verification-only — no domain/Knowledge contract changed; reuses `@polymorpha/business-logic` `typeDetector` + `@polymorpha/stella` via `git+https` G15/G15b for helpers. Suite owner `suites/polymorpha/{unit,api,e2e}` + `suites/business-logic/python` + `suites/stella` all via `node scripts/sync.mjs --check 4 SHAs`.

## Design Decisions

- Chosen — single source + G15 reuse + flake fix:
  - Single fixture source `fixtures/helpers.ts` + `fixtures/paths.ts` canonical; `suites/polymorpha/tests/mocks/helpers.ts` → `export * from "../../../fixtures/helpers"` shim (or delete and alias `vite.config.ts @mocks → fixtures` only); same `paths.ts`; delete third `g18-concurrency:5 csvPath()` local def → `import {csvPath} from "../../../fixtures/paths"`; `scripts/sync.mjs:syncFixtures copyFileSync` keeps single commit.
  - Replace `fixtures/helpers.ts:25 parseCSV` hand-roll with `import {parse} from "papaparse"` G17 (RFC4180) + `59 detectColumnType` with `import {typeDetector} from "@polymorpha/business-logic"` G15; keep `coerceValue` thin via `typeDetector` result; `buildDataset` stays test helper but deterministic via business-logic `Dataset` shape.
  - Delete `C:\` fallbacks: `suites/polymorpha/package.json:14 test:central` keep `node_modules/@polymorpha/tests/scripts/sync.mjs` + `node_modules/@polymorpha/tests` prefix only; `full-pipeline-ml:22 df_final_features.csv C:/` gated `if(!existsSync(CSV)) test.skip()` or `process.env.E2E_HEAVY_CSV`; `README.md:9` GitHub-only.
  - Wire/delete unused fixtures: add `spotify_subset,unicode,single_row,large,degenerate,mann_whitney` to `fixtures/helpers.ts` prebuilt map `fixtures.large = buildDataset(csv,"large.csv")` or document filesystem-only; or delete `spotify_subset/unicode/single_row` if no `Data Services` unit needs unicode (AGENTS matrix says `unicode.csv` for Data Services — add `unit/data-services/unicode.test.ts` then wire).
  - Rename `generators/seed.ts:20 hashString` → `seedHash/fnvHash` `number` + `g18-concurrency:26` third copy delete; where hex needed `import {hashString} from "@polymorpha/business-logic"` async.
  - Delete thin `generators/bl-bridge.ts:28` pass-through, import directly `from "./dataset" "./seed"`; fix `exports.generators` misstated `fixtures/helpers` → `fixtures/*` already.
  - Replace 4× `getMockDataset` boilerplate `process/transform/*.test.tsx` with `import {presets} from "../../generators"` `presets.mixed()` etc. (`generators/dataset.ts G20 numeric_small/wide_categorical/dirty`).
  - Split `e2e/data-modeller-dirty-10k:1354` → `data-modeller-{a-upload,b-quality,c-wrangle,d-share}.spec.ts` parallel keep `T6 concurrency>1 Promise.all 4× dirty_10k` per file isolated `describe` filter.
  - Flake fix: 13× `waitForTimeout` → `expect(locator).toBeVisible({timeout})` / `page.waitForResponse(/api\/v1\/(stats|clean)/)` / `expect.poll`; 10× `.catch(()=>{})` delete → `expect().toHaveCount(0)` fails loud G19; 10× `toHaveScreenshot` remove or `maxDiffPixelRatio` + `animations:disabled`; delete `console.log pageerror/ERRORS` debug `pipeline-smoke:8 no-react-error:14`.
  - Sync contract: `UPSTREAMS` 4th `polymorpha`→`suites` cloud-functions keep but ` .sync-sha.json` add 4th key `Polymorpha-io/polymorpha:cloud-functions` or dedup comment; `sync.mjs:161 skip missing` → `throw Error Top-level keys` G19; `vite.config.ts` single root truth with alias `@ → suites/polymorpha/src` + `@mocks → fixtures` + stella alias `@polymorpha/stella → suites/stella/ts/src` for `suites/stella/unit` ; mirrored `suites/polymorpha/vite.config.ts` → re-export shim `export {default} from "../../vite.config"`.
  - CI: `ci.yml:58 pytest suites/business-logic/python suites/stella/python suites/cloud-functions` vs only business-logic; add `sync-check needs` to `e2e` job not only `ui→e2e` chain; add `eslint` to `devDeps` or remove `lint` script.
  - Generators drift: unify TS `dirty10k` vs py `df_final`; `generators/index.ts` barrel + py `generators/__init__.py` parity via `contract.ts` generate one from other or `matrix.ts paramBuilders` already `TEST_META` driven (keep `KNOWN_MISSING_BUILDERS 10` fail-loud good but churn docs).
  - Artifacts: keep `fixtures/dirty_10k.csv 1.2MB` committed (17 CSVs total) or move to LFS/generated-on-sync `presets.dirty10k()`; `suites/polymorpha/src/` ignored correct — document `UPSTREAMS src` copy ignored `.gitignore:2`; add `git clean -fdx --dry-run` note.
- Rejected: keep `fixtures/mocks` double commit sync via `copyFileSync` — drift already `dirty_10k` vs `helpers.ts` map.
- Trade-off: `papaparse` + business-logic detector may make `fixtures/helpers` async (detector sync fine; papaparse sync `parse(csv,{header:false})`); test deterministic via seeded `seed.ts mulberry32`.
- Risk: renaming `hashString→seedHash` breaks `g18-concurrency:26` third import — grep 3 copies fix together.

## Files Changed

| File                                                                                                                         | Change                                                                            | Reason                        |
| ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ----------------------------- |
| `fixtures/helpers.ts:114` + `suites/.../mocks/helpers.ts:112` + `fixtures/paths.ts` + `mocks/paths.ts` + `g18-concurrency:5` | Single source `fixtures/`; `mocks/helpers.ts` shim `export *`                     | Byte-identical dup drift      |
| `fixtures/helpers.ts:25 parseCSV 59 detectColumnType 75 coerceValue`                                                         | `papaparse.parse` + `typeDetector` from business-logic                            | G15/G17 dup RFC4180/type heur |
| `suites/polymorpha/package.json:14 test:central one-liner` + `full-pipeline-ml:22 C:/` + `README.md:9`                       | Delete `C:/Users` fallback GitHub-only                                            | G15 violation                 |
| `fixtures/{spotify_subset,unicode,single_row}.csv` + `mocks/` mirrors + `fixtures/helpers.ts` map                            | Wire into `helpers.ts` `fixtures.*` + add `unit/data-services/unicode` or delete  | 0 hits unused                 |
| `fixtures/large.csv` `degenerate.csv mann_whitney*.csv` via `csvPath` not map                                                | Add to `helpers.ts` prebuilt or doc filesystem-only                               | API inconsistency             |
| `suites/polymorpha/tests/generators/seed.ts:20` + `g18-concurrency:26`                                                       | Rename `hashString→seedHash`                                                      | Name collision hex vs number  |
| `suites/.../generators/bl-bridge.ts:28`                                                                                      | Delete                                                                            | Pass-through 0 logic          |
| `suites/.../unit/process/transform/*.test.tsx 4 files`                                                                       | `presets.mixed()` vs `getMockDataset`                                             | Boilerplate 12L×4             |
| `suites/.../e2e/data-modeller-dirty-10k:1354`                                                                                | Split `a-upload/b-quality/c-wrangle/d-share.spec.ts`                              | Monolith 1354L                |
| `13× waitForTimeout + 10× .catch(()=>{})`                                                                                    | `toBeVisible/waitForResponse/poll`                                                | Flaky G19 hide                |
| `pipeline-smoke:8 no-react-error:14 beginner-rational 10× screenshot`                                                        | Delete `console.log` + remove/flaky screenshot                                    | Debug left + font flake       |
| `scripts/sync.mjs:32 UPSTREAMS + .sync-sha.json 3 keys + 161 skip`                                                           | Dedup/add 4th SHA, `throw Top-level keys`                                         | Stale G21 hash                |
| `vite.config.ts root vs suites/polymorpha/vite.config.ts + playwright.config.ts`                                             | Single root + shim                                                                | Alias drift                   |
| `ci.yml:58 pyproject.toml:14 testpaths`                                                                                      | `pytest suites/business-logic/python suites/stella/python suites/cloud-functions` | Coverage gap stella           |
| `generators/{dataset.ts vs python dataset.py} seed,matrix`                                                                   | Unify `dirty10k/df_final` via `contract`                                          | Drift parity                  |
| `package.json exports + lint eslint`                                                                                         | Fix `generators/*` export + add `eslint` devDep                                   | `lint` fails                  |
| `fixtures/dirty_10k.csv 1.2MB` + `suites/polymorpha/src/`                                                                    | Keep committed + doc ignored `.gitignore:2` `suites/polymorpha/src/` LFS note     | Disk bloat 1.2MB              |

## Cross-Repo Impact & Propagation (G27)

- Contracts: This repo verifies `polymorpha 11 sub-units fixtures/*.csv T6 concurrency>1` + `business-logic ts+python 347` + `stella ts 19 its + python StellaRagProfiler`. Owns `sync.mjs raw.githubusercontent never C:\Users` G22 10-point + `.sync-sha.json G21 hash truth 4 SHAs`. No domain/Knowledge primitive — thin verification layer D24.
- Pipeline: `tests feat/clean-tests → git push origin main G22` (no `business-logic/stella/polymorpha` main push needed, but `node scripts/sync.mjs --check` must pass before any `polymorpha`/`stella`/`business-logic` plan validation per G22). Consumers `polymorpha npm run test:central --check` delegates here.
- Suite owner: `suites/polymorpha/{unit:39,api:2,e2e:12} + suites/business-logic/python 17 suites 347 + suites/stella/unit 2 (19+4 its) + suites/cloud-functions 3` G21 `git ls-remote HEAD` SHA tracked.
- Opt-out: `business-logic ☐ stella ☐ polymorpha ☐ Reason: this plan cleans verification layer only — reuse of `@polymorpha`via`git+https` not changed`.
- Rollback: `git revert HEAD` on `polymorpha-tests main` + `node scripts/sync.mjs` refetch `raw.githubusercontent` `suites/`; `fixtures/helpers` single source revert restores `mocks/helpers`.
- G28: before work `git -C <each>/ pull --ff-only origin main` 4 repos match `git ls-remote HEAD`; after `git push origin main` in tests verify `polymorpha, business-logic, stella` `npm update @polymorpha/*` + `npm run build` still `sync --check 4 SHAs` green; `git status --short 0` in all 4.

## Order of Operations

1. Single-source fixtures `fixtures/helpers+paths` + shim `mocks/helpers` → `papaparse+typeDetector` G15/G17 + `seedHash` rename 3 copies → `prettier --write` D1 batch `npm run build G6` once not per edit E10.
2. Delete `C:\` fallbacks + wire unused `large/unicode` fixtures + delete `bl-bridge` + replace `getMockDataset` 4 files → `npx tsc -b --noEmit 0`.
3. Split `data-modeller-dirty-10k:1354` + fix `waitForTimeout→toBeVisible/waitForResponse` + delete `.catch/console.log/screenshot` flakes 13+10+10.
4. Fix `UPSTREAMS/.sync-sha.json` 4 SHAs + `throw` vs `console.warn` + `vite.config` single root + `ci.yml pytest stella/cloud-functions` + `package.json exports/lint` + `pyproject testpaths` alignment.
5. `node scripts/sync.mjs --check 4 SHAs` G21 hash truth + `npm run test:ui` 39 + `test:api` 2 + `test:python 347` + `test:stella 19+4` `concurrency>1` T6 + `playwright test e2e` `fixtures/*.csv` G20.

## Edge Cases & Risks

- `fixtures/helpers` papaparse sync `parse(csv,{header:false,skipEmptyLines:true})` must match hand-roll `parseLine` RFC4180 quotes; verify `unicode.csv` KaTeX `σ` not split; `detectColumnType` business-logic threshold `>=0.9` categorical same as hand-roll `59` — no drift.
- Delete `mocks/helpers` commit but `scripts/sync.mjs syncFixtures copyFileSync` copies `mocks/*.csv→fixtures/` both committed — if shim `export *` kept, `syncFixtures` becomes no-op; remove copy or keep both committed with comment `single source fixtures/`.
- Rename `hashString→seedHash`: `DatasetRepresentationService.ts` correctly `import {hashString} from "@polymorpha/business-logic"` async hex stays; `generators/seed.ts:20 fnv` stays sync number `seedHash` deterministic for `presets.dirty10k` seeded shuffle.
- Split `data-modeller-dirty-10k` 4 files: each needs `test.describe` filter + own `page` isolated not shared `POM` leak; `Promise.all 4× dirty` K99 concurrency proof stays in `d-share.spec.ts` only once.
- `waitForTimeout 1500` in `stella-knowledge:27` after `KnowledgeService.search` `EMBED_DIM 384` WASM 30MB — replace with `expect(page.locator("[data-stella-knowledge]")).toContainText("column_semantic",{timeout:15000})` not fixed 1500.
- `sync.mjs throw top-level keys` on 404 may fail CI if `raw.githubusercontent 503` — G19 requires fail not fallback `rows=[]`; add retry `fetchRaw with 3× backoff` before throw.
- `ci.yml e2e needs:ui not sync-check` — add `needs:[sync-check,ui]` so stale SHA not run E2E `concurrency>1`.
- `fixtures/dirty_10k.csv 1.2MB` keep committed now; if LFS `git lfs track fixtures/dirty_10k.csv` `suites/polymorpha/tests/.../dirty_10k 10k×15` generated-on-sync `presets.dirty10k()` would save repo size but breaks `csvPath("dirty_10k")` E2E upload — keep committed until `presets.dirty10k()` proven 1:1 `hashString` parity.
- Global scale: `ANON_MAX_ROWS 10k` slice not `403`, `storagePath users/{uid}` vs `anonymous/pending`, no `rows` shipped `>10k` `Upload/DataModeller`, `CACHE_TTL 30s LRU 50MB`, `hashDataset SHA-256`, `transaction/Lock` per-user, `concurrency>1 Promise.all`.

## Verification

- Automated: `node scripts/sync.mjs --check 4 SHAs` G21 `UPSTREAMS git ls-remote HEAD` match `.sync-sha.json` + `npm run test:ui 39` + `test:api 2` + `test:python 347 business-logic + python stella` + `vitest suites/stella/unit 19+4` + `playwright test --grep "stella|data-modeller|pipeline|workspace" T6 concurrency>1` `fixtures/{minimal,missing,dirty_10k,correlation,anova}.csv` `generators/dataset.ts:G20 numeric_small/wide_categorical/dirty`.
- Manual: `npm run sync` GitHub-only no `C:\Users` + `ls fixtures/*.csv` 17 + `suites/**/mocks/*.csv` 17 `diff -q fixtures/mocks 0` + `grep -r "C:\\\\Users" suites 0` + `grep waitForTimeout suites/polymorpha/tests/e2e 0` + `npx tsc -b --noEmit 0` `npx eslint . 0` `git diff --check 0` D27.
- Central proof: `npm run test:all` = `test:ui && test:api && test:python && test:stella` green `concurrency>1` `G18 500k merge WINDOW 1..10000 80MB LRU`; `g10-strict-inventory diff -u` dual-resident pass; `ci.yml` `pytest suites/stella/python suites/cloud-functions` now covered.
- Logs: no `console.log pageerror/ERRORS` in E2E; `sentry p95` stable; `.sync-sha.json 4 SHAs 7b25670 8b71d93 b00d82 + tests HEAD`.
